# makr-platform

Shared starter kit for MAKR Holdings Internal Tools Platform Flask apps.

Install as a private GitHub dependency:

```
makr-platform @ git+https://${GITHUB_TOKEN}@github.com/rearley/makr-platform.git@main
```

See [CLAUDE.md](CLAUDE.md) for full architecture documentation and integration guide.
